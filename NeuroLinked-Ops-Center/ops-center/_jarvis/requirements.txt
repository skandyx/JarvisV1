fastapi>=0.115.0
uvicorn[standard]>=0.32.0
anthropic>=0.39.0
httpx>=0.27.0
Pillow>=10.0.0
websockets>=13.0
psutil>=5.9.0
mcp>=1.0.0
# Multi-LLM providers (OpenAI / Groq / xAI / Ollama / Mistral / OpenRouter / Z.ai)
# sont accessibles via httpx directement, pas besoin de SDK supplémentaire.
#
# Paquets optionnels (non installés sur Linux) :
#   playwright>=1.40.0     — navigation web (nécessite: playwright install)
#   pyautogui>=0.9.54      — contrôle souris/clavier
#   pygetwindow>=0.0.9     — détection fenêtre active (Windows/macOS uniquement, NE FONCTIONNE PAS sur Linux)
#   spotipy                — contrôle Spotify
